import { describe, expect, it, beforeEach } from "bun:test";

describe("{{TestName}}", () => {
	beforeEach(() => {
		// Setup
	});

	it("should {{behavior}}", async () => {
		// Arrange

		// Act

		// Assert
	});
});
